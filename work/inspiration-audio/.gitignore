# Nothing internal may be tracked here: deploy is git pull, every tracked file is a public URL.
# Build sources live in the capture repo: ace-sites-v3/_captures/inspirationaudio-current/_build/
*.py
*.json
*.md
_*

# The nine IA playlist tracks (59MB) are NOT committed. Raw copies live in the
# capture on the SSD; the served copies are on the client's PocketBase at
# /rmc-pb/audio/. Only playlist.json (the manifest) is tracked.
audio/*.mp3
